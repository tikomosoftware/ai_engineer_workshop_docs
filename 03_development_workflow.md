# 3. 開発する・変更する

## この章の目的

AIを使って開発作業をどう進めるかを扱います。

対象は以下です。

- 新機能を追加する
- 既存機能を修正する
- 小さな仕様変更を実装する
- エラーを調査する
- ユニットテストを追加する
- PR前にセルフチェックする

テストケース作成や検証業務は4章、コードレビューの考え方やレビュー省略可否の判断は5章で扱います。

## 開発現場のAI導入レベル別の見方

開発でのAI活用は、現場によってかなり差があります。

そのため、この章の内容は「AIをどう使える環境か」によって読み替えます。

| 現場の状態 | AIの使い方 | この章で特に見るところ |
|---|---|---|
| チャット型AI中心 | 仕様、コード、エラー、差分を人間が貼り付けて相談する | 実装前整理、エラー調査、PR前セルフチェック |
| IDE / ローカルAIエージェント利用 | AIがプロジェクトを読み、コードを書き、テストを実行する | ユニットテスト追加、テスト実行、失敗時の修正 |
| GitHub / CI連携AIエージェント利用 | Issue、PR、CI失敗を起点に、AIが計画・修正・テスト・Draft PR作成まで行う | CI/CD補足、PR前セルフチェック、人間の確認観点 |
| AI前提で開発体制を作り替えられる | 役割分担やレビュー、CI/CD、リリース判断までAI前提で再設計する | この資料の主対象外。別テーマとして整理する |

この資料では、すべての現場が一気にGitHub / CI連携AIエージェントまで進む前提にはしません。

一方で、すでにAIがテスト実行やPR作成まで行う環境も出てきています。その場合でも、差分確認、テストの意味、影響範囲、マージ・リリース判断は人間が確認します。

## 3-1. 開発フェーズでAIを使う目的

AIを使う目的は、単にコードを書かせることではありません。

- 実装前：仕様、変更範囲、影響範囲を整理する
- 実装中：エラー調査、実装方針相談、コード理解を行う
- 実装後：ユニットテスト追加、PR前セルフチェックを行う

## 3-2. 実装前：仕様と変更範囲を整理する

いきなりコードを書かせる前に、仕様や影響範囲を整理させます。

確認したいことは以下です。

- 実装方針
- 影響を受けそうな機能
- 変更が必要そうなファイル
- DBへの影響
- APIへの影響
- 画面への影響
- 注意すべき既存仕様
- 不明点

### プロンプト例

```text
以下の仕様変更について、実装前に整理してください。

対象：
[対象機能]

変更内容：
[仕様変更の内容]

知りたいこと：
1. 実装方針
2. 影響を受けそうな機能
3. 変更が必要そうなファイル
4. DB・API・画面への影響
5. 注意すべき既存仕様
6. 不明点・確認すべきこと

条件：
- 推測と事実を分ける
- 不明点は不明と書く
- まず実装コードは書かず、整理だけしてください
```

## 3-3. 実装中：開発中のエラー調査

開発中のエラーはAIが得意な領域です。

例：

- TypeError
- SQLSTATE
- Class not found
- Unresolved reference
- Target of URI doesn't exist
- Build failed
- Migration error
- Import error
- 依存関係エラー

AIに渡す情報は以下です。

- エラーメッセージ全文
- 実行コマンド
- 該当コード
- 使用技術
- バージョン
- 直前に変更した内容
- 期待していた動作

### プロンプト例

```text
以下のエラーについて原因候補を教えてください。

環境：
- Laravel 11
- PHP 8.3
- MySQL
- ローカル開発環境

実行したこと：
php artisan migrate

エラー：
[ここにエラー全文]

知りたいこと：
1. 原因候補
2. 最初に確認すべきこと
3. 修正手順
4. 再発防止の観点
```

## 3-4. 開発中エラーと運用中ログは別物

開発中は、比較的シンプルに次のように聞けます。

```text
このエラーの原因と修正方法を教えてください。
```

一方で、運用中の本番ログでは、原因を断定させるのではなく、切り分けを整理させる必要があります。

```text
原因を断定せず、切り分け観点と次の確認手順を整理してください。
```

この違いは運用保守の章で詳しく扱います。

## 3-5. 実装後：開発者目線の確認観点を作る

実装後は、PR前セルフチェックの一部として、開発者目線の確認観点を作ります。

本格的なテストケース作成、差分からのテスト抽出、ブラウザ操作による検証は4章で扱います。

出させる観点：

- 正常系
- 異常系
- 境界値
- 権限
- 入力チェック
- 既存機能への影響
- 回帰テスト
- 外部API失敗
- DB更新失敗
- 並行処理
- パフォーマンス

### プロンプト例

```text
以下の仕様変更について、開発者がPR前に確認すべき観点を出してください。

対象：
ユーザー登録画面

変更内容：
メールアドレスの重複チェックを追加

出してほしいもの：
- 正常系
- 異常系
- 境界値
- 既存機能への影響
- 回帰テスト観点
- 権限に関する確認
- DB更新に関する確認

出力：
表形式で、優先度 High / Medium / Low を付けてください。
```

## 3-6. 実装後：ユニットテストを追加する

実装後は、変更した処理に対してユニットテストを追加します。

最近のAIエージェントでは、テスト観点の洗い出しだけでなく、既存テストに合わせたテストコードの作成、テスト実行、失敗時の原因調査、修正、再実行まで任せられる場面があります。

流れとしては、以下のようになります。

```text
テスト観点を出す
↓
テストコードを書く
↓
テストを実行する
↓
失敗したら原因を調べる
↓
実装またはテストを修正する
↓
再実行する
```

ただし、テストが通ったことと、仕様を満たしていることは別です。AIが作ったテストが、本当に確認すべき仕様を確認できているかは人間が見ます。

ユニットテストで確認したいことは以下です。

- 正常系
- 異常系
- 境界値
- null / undefined / 空文字 / 空配列
- 例外条件
- 権限や状態による分岐
- 外部依存のモック
- 既存仕様を壊していないか

### テスト観点を出すプロンプト

```text
以下の変更について、追加すべきユニットテストの観点を出してください。

対象：
[対象ファイル・関数・クラス]

変更内容：
[変更内容]

知りたいこと：
1. 正常系
2. 異常系
3. 境界値
4. 例外条件
5. モックが必要な依存関係
6. 既存テストへの追加方針

条件：
- 既存の仕様と推測を分ける
- 優先度 High / Medium / Low を付ける
- テストコードはまだ書かず、まず観点だけ整理する
```

### テストコードのたたき台を作るプロンプト

```text
以下の既存テストの書き方に合わせて、今回の変更に対するユニットテストを作成し、可能であればテストを実行してください。

対象：
[対象ファイル・関数・クラス]

変更内容：
[変更内容]

既存テスト：
[既存テストコード]

条件：
- 既存のテストスタイルに合わせる
- テスト名は何を確認しているか分かる名前にする
- 必要なモックを明示する
- テストを実行できる環境なら実行する
- 失敗した場合は原因候補を整理する
- 不明な依存関係は不明と書く
```

### テスト失敗時の調査プロンプト

```text
以下のユニットテスト失敗について、原因候補と確認手順を整理してください。

対象：
[対象テスト]

実行コマンド：
[コマンド]

エラー：
[エラー全文]

直前に変更した内容：
[変更内容]

知りたいこと：
1. 原因候補
2. 最初に確認すべきこと
3. テスト側の問題か、実装側の問題か
4. 修正方針
```

### テストを通すときの注意点

AIにテスト実行と修正まで任せる場合でも、以下は人間が確認します。

- テストが本来の仕様を確認しているか
- テストを通すために期待値を弱めていないか
- 実装に合わせてテストを書き換えていないか
- モックしすぎて意味のないテストになっていないか
- 失敗していたテストを削除していないか
- 既存仕様を壊していないか

> テストが通ったことと、仕様を満たしていることは別です。  
> AIにはテスト実行まで任せられる場合がありますが、テストの意味は人間が確認します。

## 3-7. PR前セルフチェック

PR前セルフチェックは、人間レビューに出す前に、明らかなミスや確認漏れを減らすために行います。レビューの完全な代替というより、粗取りです。

確認観点：

- 実行時エラー
- null / undefined / 空配列
- データ不整合
- 認可漏れ
- 既存仕様を壊す可能性
- テスト不足
- エラー処理不足

### プロンプト例

```text
以下の差分について、PR提出前のセルフチェックをしてください。

目的：
人間レビューに出す前に、重大な不具合や確認漏れを減らすこと。

対象：
今回の変更差分のみ

確認観点：
- 実行時エラー
- データ不整合
- 認可漏れ
- 既存仕様を壊す可能性
- 追加すべきテスト観点

除外：
- 命名の好み
- 軽微なリファクタリング
- コメント追加の提案
- 将来の拡張性だけの話

出力：
最大5件。
High / Medium / Low で分類。
High と Medium だけ対応候補にしてください。
```

## 3-8. AIレビューは制限しないと終わらない

PR前セルフチェックは便利ですが、聞き方を広げすぎると、開発作業としては扱いにくくなります。

AIに以下のように頼むと、指摘が広がりやすいです。

```text
このコードをレビューしてください。
```

AIは親切なので、以下のような指摘を出しがちです。

```text
命名がもっと良くなる
コメントを追加した方がよい
例外処理を考えた方がよい
ログ出力を追加した方がよい
テストを増やした方がよい
パフォーマンスを考えた方がよい
将来の拡張性を考えた方がよい
```

どれも間違いではありませんが、全部対応していたら終わりません。

そのため、この章ではAIレビュー全般ではなく、開発者自身のPR前セルフチェックとして観点を限定します。

コードレビューの目的、レビュー省略可否、Must / Should / Nits の分類は、5章で扱います。

## 3-9. 補足：CI/CDとリポジトリ連携AIの現状

CI/CDにもAI活用は入り始めています。ただし、この資料ではCI/CDそのものは主な対象にしません。

CI/CD周りのAI活用は、現場の開発基盤によってかなり差があります。

| 現場の状態 | AIの使い方 |
|---|---|
| CI/CDとは未連携 | CI失敗ログを人間がAIに渡して、原因候補を整理する |
| CI/CDツールにAI補助あり | 失敗ログの要約、原因候補、flaky test の分類などをAIが補助する |
| GitHub / PR連携AIあり | Issue、PR、CI失敗を起点に、AIが修正案やDraft PRを作成する |
| デプロイ判断まで自動化する環境 | この資料の主対象外。承認、監査、リリース責任を含めて別途設計する |

この資料で扱うのは、主に以下のような補助・開発支援としての使い方です。

- CI失敗ログの要約
- 失敗原因の候補整理
- テスト失敗の分類
- flaky test の調査補助
- CI/CD設定ファイルの作成・修正支援
- デプロイ失敗時の調査補助
- セキュリティスキャン結果の優先度整理
- Issue / PR / CI失敗を起点に、AI coding agent が修正案やPRを作成する

最近は、GitHub上のIssueやPull RequestをAI coding agentに割り当て、AIが計画を立て、コードを変更し、テストやLintを実行し、Draft PRを作成する流れも出てきています。

また、CI失敗やコードスキャン結果を起点に、AIが原因候補を整理したり、修正案を出したり、場合によっては修正PRを作成する方向も進んでいます。

そのため、今後は「ローカルで自分がテストを回して確認する」だけでなく、GitHubなどの開発基盤上で、AIがIssue、PR、CI結果を読んで作業計画や修正タスクを進める形が増える可能性があります。

ただし、この場合もAIが勝手に本番反映まで行う前提にはしません。

```text
Issue / PR / CI失敗
↓
AIが原因候補や修正方針を整理する
↓
AIがブランチやDraft PRを作成する
↓
AIがテストやLintを実行する
↓
人間が差分・テスト結果・影響範囲を確認する
↓
人間がマージやリリースを判断する
```

CI/CDは本番反映、ロールバック、承認フロー、監査に関わるため、AIの提案をそのまま適用するのではなく、人間の確認・承認を挟みます。

開発作業の流れとしては、CI/CDの失敗ログをAIに読ませて原因候補を整理したり、設定ファイルの修正案を出させたり、IssueやPRを起点に修正PRを作らせたりする使い方が現実的です。

参考：

- [GitLab Fix CI/CD Pipeline Flow](https://docs.gitlab.com/user/duo_agent_platform/flows/fix_pipeline/)
- [GitLab Duo CI/CD root cause analysis](https://about.gitlab.com/blog/developing-gitlab-duo-blending-ai-and-root-cause-analysis-to-fix-ci-cd/)
- [Harness AIDA for CI troubleshooting](https://developer.harness.io/docs/continuous-integration/troubleshoot-ci/aida/)
- [GitHub Copilot coding agent](https://docs.github.com/en/copilot/using-github-copilot/coding-agent/about-assigning-tasks-to-copilot)
- [Ask Copilot to make changes to a pull request](https://github.blog/changelog/2026-03-24-ask-copilot-to-make-changes-to-any-pull-request)
- [Using GitHub Copilot code review](https://docs.github.com/en/copilot/how-tos/use-copilot-agents/request-a-code-review/use-code-review)
- [GitHub Copilot pull request summaries](https://docs.github.com/en/copilot/responsible-use/pull-request-summaries)
- [CircleCI Test Insights](https://circleci.com/docs/guides/insights/insights-tests/)

## 3-10. この章のまとめ

```text
開発フェーズでは、AIを実装そのものだけに使わない。

実装前：
仕様と影響範囲を整理する

実装中：
エラー調査を行う

実装後：
ユニットテストを追加する
PR前セルフチェックを行う

CI/CDでのAI活用は、失敗ログ分析、設定修正支援、Issue/PR起点の修正PR作成などの補助として扱う。

テスト・検証、レビュー方針の整理は別章で扱う。
```
