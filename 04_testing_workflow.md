# 4. テスト・検証する

## この章の目的

AIを使って、テスト設計・テストケース作成・テスト実施・自動テスト作成をどう進めるかを扱います。

対象となる場面は以下です。

- 仕様や既存コードからテストケースを作る
- Gitの差分から必要なテスト観点を洗い出す
- 最低限確認すべきテストケースを作る
- 既存プロジェクトの主要業務フローに対するテスト観点を作る
- ブラウザ操作を伴うテストを自動化する
- ユニットテストや結合テストを作成・実行する
- テスト結果や不具合内容を整理する

開発者が実装後に行うテスト観点作成は3章でも扱います。この章では、よりテスター業務・検証業務に寄せて、テストケース作成とテスト実施を扱います。

## テスト現場のAI導入レベル別の見方

テスト・検証でのAI活用も、現場の環境によって大きく変わります。

| 現場の状態 | AIの使い方 | この章で特に見るところ |
|---|---|---|
| チャット型AI中心 | 仕様、差分、画面仕様を渡してテストケースを作る | 仕様からのテストケース作成、Git差分からのテスト抽出 |
| プロジェクト読込可能 | AIが既存コードや画面/API構成を読んで、主要業務フローのテスト観点を作る | 主要業務フローからのテスト観点作成 |
| ブラウザ操作可能 | AIや自動化ツールが画面を操作して確認する | ブラウザ操作を伴うテスト、自動化しやすい観点 |
| テスト実行可能 | AIがユニットテスト、結合テスト、E2Eテストを実行し、失敗原因を調査する | 自動テスト、失敗時の原因整理 |
| CI / PR連携あり | CI失敗やPR差分を起点に、AIがテスト結果を整理し、追加確認や修正案を出す | Git差分からのテスト抽出、必要最低限のテストケース |

AIがどこまで実行できるかによって、テスターの役割も変わります。

AIがテストケースを作るだけの現場では、人間が実施と判断を行います。AIがブラウザ操作やテスト実行までできる現場では、人間は「テストが本当に仕様を確認できているか」「確認すべき業務リスクが抜けていないか」を見る比重が増えます。

## 4-1. テスター業務でAIを使う目的

テスター業務でAIを使う目的は、単にテストケースを大量に出すことではありません。

- 仕様やコードから確認観点を漏れなく洗い出す
- 変更差分から影響範囲を考える
- 必要最低限のテストケースを絞り込む
- 既存プロジェクトの主要業務フローを理解する
- ブラウザ操作やユニットテストを自動化・実行する
- 不具合報告や再現手順を整理する

AIに任せたいのは、テストの判断そのものではなく、テスト観点の整理、抜け漏れ確認、実施手順のたたき台作成です。

## 4-2. 仕様からテストケースを作る

仕様書やチケットの内容から、正常系・異常系・境界値・権限・既存機能への影響を洗い出します。

### プロンプト例

```text
以下の仕様について、テストケースを作成してください。

対象機能：
[対象機能]

仕様：
[仕様内容]

出してほしいもの：
1. 正常系
2. 異常系
3. 境界値
4. 権限
5. 入力チェック
6. 既存機能への影響
7. 回帰テスト観点

条件：
- テスト観点とテスト手順を分ける
- 優先度 High / Medium / Low を付ける
- 期待結果を書く
- 不明点は不明点としてまとめる
```

## 4-3. Gitの差分から必要なテストケースを作る

差分がある場合は、AIに変更内容を要約させたうえで、必要なテストケースを作らせます。

このとき、網羅的なテストケースだけでなく、必要最低限の確認観点も出させると実務で使いやすくなります。

### プロンプト例

```text
以下のGit差分から、今回確認すべきテストケースを作成してください。

目的：
PR前またはリリース前に、変更差分に対して必要なテストを整理すること。

対象：
今回の変更差分のみ

出してほしいもの：
1. 変更内容の要約
2. 影響を受けそうな機能
3. 必ず確認すべきテストケース
4. 余裕があれば確認したいテストケース
5. 回帰テスト観点
6. テスト不要と判断できそうな範囲

条件：
- 事実と推測を分ける
- 重要度 High / Medium / Low を付ける
- 必要最低限のテストケースも別枠で出す
- 既存コードの改善提案は不要
```

## 4-4. 既存プロジェクトの主要業務フローからテスト観点を作る

既存プロジェクトでは、仕様書が古い、または不足していることがあります。その場合は、AIにプロジェクトを読ませて、主要業務フローごとのテスト観点を整理させます。

### プロンプト例

```text
このプロジェクトの主要業務フローを整理し、各業務フローに対するテスト観点を作成してください。

知りたいこと：
1. 主要業務フロー
2. 各フローの入口画面/API/バッチ
3. 正常系テスト
4. 異常系テスト
5. 権限・認可の確認
6. DB更新や外部連携の確認
7. 回帰テスト観点
8. 変更時に影響しやすい箇所

条件：
- 根拠ファイルを示す
- 事実と推測を分ける
- 不明点は不明と書く
- テストケースは優先度順に並べる
```

## 4-5. 必要最低限のテストケースを作る

時間が限られている場合、全観点を網羅するのではなく、リスクの高いところを優先して確認します。

AIには「全部出す」だけでなく、「最低限どこを確認するか」を聞きます。

### プロンプト例

```text
以下の変更について、時間が限られている場合に最低限確認すべきテストケースを出してください。

変更内容：
[変更内容]

前提：
- リリース前の確認
- テスト時間が限られている
- 重大な不具合を避けたい

出してほしいもの：
1. 最低限確認すべきテストケース
2. その理由
3. 確認しない場合のリスク
4. 余裕があれば追加で確認するテストケース

条件：
- High / Medium / Low で優先度を付ける
- High のみで構成した最小セットを出す
- 判断に迷うものは「判断保留」とする
```

## 4-6. ブラウザ操作を伴うテストを自動化する

Webアプリでは、ブラウザ操作をAIや自動化ツールで実行できる場合があります。たとえば、画面遷移、フォーム入力、検索、登録、更新、削除、バリデーション表示などです。

Playwrightやブラウザ操作ツールを使える環境では、AIにテスト観点を出させるだけでなく、実際に画面を操作して確認することもできます。

### 使いどころ

- 画面表示の確認
- フォーム入力
- バリデーション確認
- 画面遷移
- 権限ごとの表示差分
- 主要業務フローの疎通確認
- リリース前の簡易回帰確認

### プロンプト例

```text
以下の画面について、ブラウザ操作で確認するテスト手順を作成してください。

対象画面：
[画面名]

確認したいこと：
- 正常に表示されるか
- 必須入力チェックが動くか
- 登録できるか
- 登録後の一覧に反映されるか
- エラー時に適切なメッセージが表示されるか

出力：
1. テスト手順
2. 入力データ
3. 期待結果
4. 自動化しやすい観点
5. 人間が目視確認すべき観点
```

## 4-7. ユニットテストや結合テストを作成・実行する

ユニットテストや結合テストは、開発者の作業にもテスターの支援にも関係します。

AIには、仕様や関数の責務からテストパターンを出させたり、既存テストの書き方に合わせたテストコードを作らせたりできます。テスト実行ができる環境では、実行結果の確認や失敗原因の整理まで任せられる場合があります。

### プロンプト例

```text
以下の関数について、ユニットテストの観点とテストコードを作成し、実行できる環境ならテストを実行してください。

対象：
[関数またはファイル]

知りたいこと：
1. 正常系
2. 異常系
3. 境界値
4. 例外条件
5. モックが必要な依存関係
6. 既存テストに合わせた書き方

条件：
- 既存のテストコードのスタイルに合わせる
- テスト名は何を確認しているか分かる名前にする
- 実行できる環境なら実行する
- 失敗した場合は原因候補を整理する
- 不明な依存関係は不明と書く
```

テストを実行できる場合でも、テストが通ったことと仕様を満たしていることは別です。AIが作成したテストが、本当に確認すべき仕様や業務条件を確認できているかは人間が確認します。

## 4-8. 不具合報告や再現手順を整理する

テストで見つけた不具合は、再現手順、期待結果、実際の結果、環境、影響範囲を整理して報告します。

AIは、テストメモから不具合報告の文章を整える用途にも使えます。

### プロンプト例

```text
以下のテストメモをもとに、不具合報告を作成してください。

テストメモ：
[メモ]

出してほしいもの：
1. タイトル
2. 発生環境
3. 再現手順
4. 期待結果
5. 実際の結果
6. 影響範囲
7. 添付すべき証跡
8. 追加で確認すべきこと

条件：
- 原因を断定しない
- 開発者が再現しやすいように書く
- 個人情報や顧客情報はマスキングする
```

## 4-9. テストでAIを使うときの注意点

AIが出したテストケースは、網羅性があるように見えても、仕様や業務理解が不足している場合があります。

注意すること：

- AIが出したテストケースをそのまま正解にしない
- 仕様、業務フロー、過去障害、リリース範囲と照らす
- 自動テストだけで目視確認を完全に置き換えない
- テストデータに個人情報や顧客情報を含めない
- 本番環境で自動操作しない
- 破壊的な操作や大量データ操作は人間が確認する
- AIが作ったユニットテストは実行して確認する

## 4-10. この章のまとめ

```text
テスター業務では、AIをテストケース作成だけに使わない。

仕様からテスト観点を出す
差分から必要なテストを絞る
主要業務フローから回帰観点を作る
ブラウザ操作やユニットテストを自動化・実行する
不具合報告を整理する

ただし、AIが出したテストケースは候補であり、
最終的なテスト判断は人間が行う。
```
