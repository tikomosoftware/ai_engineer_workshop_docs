# 補足：AIが使えない環境での検索・汎化メモ

## この補足の位置づけ

この補足は、未許可のAIツールを業務利用するための手順ではありません。

業務でAIを使う場合は、会社またはプロジェクトで許可されたAIツールを使うことが前提です。未許可AI、個人アカウント、無料版AIを業務利用することは原則NGとして扱います。

ただし、現場によっては許可されたAIツールを使えない環境や、AIではなく通常の検索エンジンで技術調査を行う場面があります。その場合、検索キーワードや外部サービスに入力する内容にも注意が必要です。

この補足では、AI利用というより、外部サービスに情報を入力するときのマスキング・汎化の考え方を整理します。

## 基本方針

外部サービスに入力する前に、情報を以下のように分けます。

| 分類 | 扱い |
|---|---|
| そのまま検索・相談しやすい情報 | 公開されているエラー名、一般的な技術名、公式ドキュメントに載っている仕様 |
| 汎化して使う情報 | 社内固有名詞、顧客名、プロジェクト名、具体的な業務フロー |
| マスキングして使う情報 | ログ、問い合わせ内容、設定値、パス、ID、IPアドレス |
| 入力しない情報 | APIキー、アクセストークン、秘密鍵、パスワード、接続文字列、個人情報の生データ |

## 検索キーワードの汎化

検索エンジンに入力する場合も、社内名や顧客名をそのまま入れないようにします。

悪い例：

```text
〇〇社 顧客管理システム ログイン 500エラー user_id=12345
```

良い例：

```text
Laravel login 500 error session middleware
```

良い例では、顧客名、社内システム名、ユーザーIDを落として、技術的な要素だけにしています。

## ログのマスキング例

悪い例：

```text
user_id=12345 email=user@example.com token=abcd1234 ip=203.0.113.10
```

良い例：

```text
user_id=[USER_ID] email=[EMAIL] token=[TOKEN] ip=[IP_ADDRESS]
```

## 仕様や業務内容の汎化例

悪い例：

```text
〇〇社の請求締め処理で、取引先Aだけ請求金額がずれる
```

良い例：

```text
月次の集計処理で、特定の取引先だけ集計金額がずれる。
原因候補として、丸め処理、税率、締め日、除外条件を確認したい。
```

## AIに聞けない場合の聞き方

許可されたAIツールが使えない場合でも、外部検索や公式ドキュメント調査は行うことがあります。その場合は、具体情報を落として、技術要素に分解します。

例：

```text
技術：
バックエンドフレームワーク

症状：
ログイン後にセッションが維持されない

確認したいこと：
- セッション設定
- Cookie設定
- CSRF
- リバースプロキシ
- HTTPS設定
```

## 注意点

- 未許可AI、個人アカウント、無料版AIを業務利用しない
- 検索キーワードにも社内情報・顧客情報・個人情報を入れない
- APIキー、秘密鍵、アクセストークン、パスワードは入力しない
- ログは必要最小限に切り出す
- 技術名、エラー名、一般化した症状に分解して調べる
- 判断が必要な場合は、社内の許可されたAIツールや関係者に戻す

## この補足のまとめ

```text
未許可AIを業務利用するための話ではない。

許可されたAIが使えない環境や、通常の検索を行う場面では、
社内情報・顧客情報・個人情報・認証情報をそのまま外部に入力しない。

検索や相談に使う情報は、
技術名、エラー名、一般化した症状、マスキング済みログに寄せる。
```
