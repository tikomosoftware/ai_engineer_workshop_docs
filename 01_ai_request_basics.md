# 1. AIへの依頼方法の前提整理

## この章の目的

AI活用の最初に、AIへの依頼方法の誤解を解きます。特に、次の誤解を先に解消します。

> 最近のAIは賢いから、細かく指示しなくていい。だからプロンプトを考えなくていい。

この理解は少し危険です。正しくは次のように整理します。

> 昔のような呪文プロンプトは不要になってきた。しかし、実務では依頼条件の整理は今でも重要。

## 1-1. この章で先に伝えたいこと

最近、「AIには細かく指示しなくていい」と言われることがあります。

これは、昔のような「あなたは最高のエンジニアです」のような呪文が不要になってきた、という意味では正しいです。

ただし、実務では「何を対象にするか」「何を見てほしいか」「何を見なくていいか」を伝えないと、AIの回答は広がりすぎます。

この章では、プロンプト職人になる話ではなく、AIに仕事を頼むときの条件整理を扱います。

プロンプトそのものを否定しているわけではありません。たとえば画像や動画など、プログラミング以外の分野では、プロンプトの工夫が有効な場面もあります。

## 1-2. 「細かく指示しなくていい」の誤解

最近よくある言い方として、以下があります。

- 最近のAIは賢いから、細かく指示しなくていい
- プロンプトを考える時代は終わった
- 短く頼めばAIがよしなにやってくれる
- AIにはやりたいことだけ伝えればいい

これは半分正しいですが、実務では誤解されやすいです。

正しくは以下です。

> 昔のような、AIをうまく誘導するための呪文的なプロンプトは不要になってきた。  
> しかし、実務で使うなら、目的・対象・前提・制約・除外・出力形式は今でも必要。

## 1-3. 「詳細なプロンプトは不要」という誤解

よくある誤解として、次のようなものがあります。

> 「詳細なプロンプトは不要」 != 「プロンプトを短くする」「プロンプトを考えない」

「詳細なプロンプトは不要」とは、AIに丸投げするとか、プロンプトを短くするという意味ではありません。余計なおまじないは減らしつつ、仕事の依頼条件は明確にするということです。

### 悪い方向の理解

- プロンプトは短くすればよい
- プロンプトは考えなくてよい
- AIが賢いから雑に頼んでもよい

### 良い方向の理解

- 余計なおまじないは減らす
- ただし目的・対象・制約は書く
- 実務では「何をしないか」も指定する
- 出力の使い道に合わせて頼む

### 昔と今の違い

昔のプロンプトでは、AIをうまく動かすために、役割や思考手順を細かく書くことが多くありました。

```text
あなたは最高のエンジニアです。
深呼吸して、ステップバイステップで考えてください。
```

最近のAIでは、このような呪文的な指定の重要度は下がっています。特に、AIに内部の思考手順を細かく指定するよりも、実務上の依頼条件を明確にした方が使いやすいです。

細かく言うべきなのは、AIの思考手順ではありません。**見てほしい観点、制約、出力条件、判断保留の扱い**です。

| 昔のプロンプトでやりがちなこと | 今の実務で重視したいこと |
|---|---|
| 役割や人格を長く指定する | 目的と対象を明確にする |
| 思考手順を細かく指定する | 見てほしい観点を指定する |
| 「問題ありますか？」とだけ聞く | 重要度、件数、根拠、確認方法を指定する |
| 「レビューしてください」と広く頼む | 見る範囲と除外する観点を指定する |
| AIの同意をそのまま受け取る | 判断保留、反対意見、代替案を出させる |

たとえば、セキュリティ確認では「セキュリティチェックしてください」だけではなく、認証・認可、入力値検証、ログ出力などの観点と、High / Medium / Low の出力形式を指定します。

設計相談では「この方針でよいですか？」だけではなく、同意する前に懸念点、矛盾、見落とし、代替案を出させます。

## 1-4. プロンプトは作文力ではなく依頼設計

エンジニア向けには、次の言い方が分かりやすいです。

> プロンプトは作文力ではなく、依頼設計です。要件定義のミニ版として考えるとよいです。

人間に仕事を頼むときと同じです。「このコード見ておいて」だけでは、相手も困ります。

- 何を見ればよいのか
- どこまで見るのか
- バグを見るのか
- 設計を見るのか
- 命名を見るのか
- セキュリティを見るのか
- 何件くらい指摘すればよいのか
- 修正案まで必要なのか

これらがないと、回答が広がります。AIも同じです。

## 1-5. AIへの依頼の基本形

```text
目的：
対象：
前提：
制約：
除外：
出力形式：
```

| 項目 | 意味 |
|---|---|
| 目的 | 何のためにAIに依頼するのか |
| 対象 | 何を見るのか。差分、ファイル、ログ、仕様など |
| 前提 | 技術スタック、環境、制約、状況 |
| 制約 | 最大件数、対象範囲、優先度など |
| 除外 | 見なくてよいこと、不要な指摘 |
| 出力形式 | 表、箇条書き、High/Medium/Low分類など |

## 1-6. 悪い例と良い例

### 悪い例

```text
このコードレビューして
```

これでもAIは回答しますが、以下の問題が起きやすいです。

- 命名の好みまで指摘する
- 軽微なリファクタリングを提案する
- 将来の拡張性の話を広げる
- 重要度の低い指摘が増える
- レビューが終わらない
- どこまで対応すべきか分からない

### 良い例

```text
目的：
PR前のセルフチェック

対象：
今回の変更差分のみ

確認観点：
重大な不具合につながる可能性

除外：
命名の好み、軽微なリファクタリング、将来の拡張性だけの話

出力：
最大5件。High / Medium / Low で分類
High と Medium だけ対応候補にしてください
```

## 1-7. 「除外」が特に重要

AIは親切なので、放っておくとどんどん観点を広げます。特にコードレビューやセルフチェックでは、以下の除外条件が有効です。

```text
命名の好みは不要です。
軽微なリファクタリング提案は不要です。
コメント追加の提案は不要です。
将来の拡張性だけの話は不要です。
今回の変更差分に関係ない既存コード改善は不要です。
```

## 1-8. 実務でよく使う依頼パターン

### 既存コード理解

```text
このプロジェクトを初めて読むエンジニア向けに説明してください。

知りたいこと：
1. 何をするアプリか
2. 使用技術
3. 主要機能
4. 処理の入口
5. 最初に読むべきファイル

条件：
- 事実と推測を分ける
- 根拠ファイルを示す
- 不明点は不明と書く
```

### エラー調査

```text
以下のエラーについて原因候補を教えてください。

環境：
[技術スタック・バージョン]

実行したこと：
[コマンドや操作]

エラー：
[エラー全文]

知りたいこと：
1. 原因候補
2. 最初に確認すべきこと
3. 修正手順
4. 再発防止の観点
```

### 運用ログ調査

```text
以下は本番環境で発生したエラーログです。
入力可否は社内ルール確認済みです。
外部サービスに出せない情報はマスキング・汎化済みです。

依頼：
1. 原因候補を可能性が高い順に出してください
2. 断定はしないでください
3. 次に確認すべきログ、メトリクス、DB、外部APIを分けてください
4. 一次報告に使える文章も作ってください
```

### テスト観点作成

```text
以下の仕様変更について、テスト観点を出してください。

対象：
[対象機能]

変更内容：
[変更内容]

出してほしいもの：
- 正常系
- 異常系
- 境界値
- 権限
- 既存機能への影響
- 回帰テスト観点

出力：
表形式で、優先度 High / Medium / Low を付けてください。
```

### セキュリティ確認

セキュリティ確認では、単に「セキュリティチェックしてください」と頼むと、AIが浅く見て「大きな問題はありません」と返すことがあります。

実務では、問題がある前提で探させるのではなく、**問題がないと結論づける前に、どの観点を確認したかを出させる**ようにします。

悪い例：

```text
このコードをセキュリティチェックしてください。
```

良い例：

```text
以下の差分を、セキュリティ観点で確認してください。

目的：
PR前に、重大なセキュリティリスクや確認漏れを洗い出すこと。

対象：
今回の変更差分のみ

確認してほしい観点：
- 認証・認可
- 個人情報・機密情報の扱い
- 入力値検証
- SQLインジェクション
- XSS
- CSRF
- ファイルアップロード
- 外部API連携
- ログ出力
- 権限昇格

出力：
- High / Medium / Low に分ける
- 各重要度で最大3件まで挙げる
- 指摘ごとに、根拠、想定される影響、確認方法、修正方針を書く
- 判断できないものは「判断保留」として、追加で必要な情報を書く

注意：
- 問題なしと結論づける前に、確認した観点を列挙してください
- 推測とコードから確認できる事実を分けてください
- 命名や軽微なリファクタリングの指摘は不要です
```

ポイントは、「問題があるか」だけを聞かず、重要度、最大件数、確認観点、根拠、判断保留を指定することです。

### 批判的に見てもらう

AIは、こちらの案に同意する方向で回答することがあります。そのため、設計方針や実装方針を相談するときは、最初から反対意見やリスクを出させると実務で使いやすくなります。

```text
以下の方針案について、同意する前に批判的に確認してください。

方針案：
[ここに方針を書く]

確認してほしいこと：
1. 前提が不足している点
2. 論理的に飛躍している点
3. 実装・運用・セキュリティ上のリスク
4. 代替案
5. この方針に同意できる条件

出力：
- まず懸念点を優先して書く
- その後、妥当だと言える点を書く
- 事実、推測、意見を分ける
- 分からないことは分からないと書く
- 最後に、採用 / 修正して採用 / 判断保留 / 不採用 のどれかでまとめる
```

この聞き方にすると、AIがただ同意するのではなく、前提、リスク、代替案を出しやすくなります。

### カスタムインストラクション例

よく使うAIツールにカスタムインストラクションを設定できる場合は、以下のような方針を入れておくと、安易な同意を減らしやすくなります。

```text
ユーザーに同意する場合は、論理的に妥当であり、根拠や前提と矛盾しないと判断できる場合に限ってください。

ユーザーの前提が不足している、誤っている、リスクがある、または別解がありそうな場合は、先にその点を指摘してください。

回答では、事実、推測、意見を分けてください。

設計、実装、レビュー、運用、セキュリティに関する相談では、最初に懸念点、矛盾、見落とし、トレードオフを確認してください。

不明な点がある場合は、無理に断定せず、何が分かれば判断できるかを書いてください。
```

ただし、カスタムインストラクションを入れても、AIの回答が常に正しくなるわけではありません。AIが批判的に見た結果も、最終的には人間が根拠と実態に照らして確認します。


## 1-9. この章のまとめ

```text
プロンプトは短くすればよいわけではない。
プロンプトを考えなくてよいわけでもない。

不要なのは呪文的なプロンプト。
必要なのは実務上の依頼条件。

AIへの依頼は、目的・対象・前提・制約・除外・出力形式を整理すること。
AIに批判的に見てほしい場合は、重要度、確認観点、根拠、判断保留、反対意見を明示すること。
細かく指定するのは、AIの思考手順ではなく、見てほしい観点、制約、出力条件、判断保留の扱い。
```
